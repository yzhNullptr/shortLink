<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.yzh.project.dao.mapper.LinkAccessStatsMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="org.yzh.project.dao.entity.LinkAccessStatsDO" id="LinkAccessStatsMap">
        <result property="gid" column="gid"/>
        <result property="fullShortUrl" column="fullShortUrl"/>
        <result property="date" column="date"/>
        <result property="pv" column="pv"/>
        <result property="uv" column="uv"/>
        <result property="uip" column="uip"/>
        <result property="hour" column="hour"/>
        <result property="weekday" column="weekday"/>
        <result property="createTime" column="createTime"/>
        <result property="updateTime" column="updateTime"/>
        <result property="delFlag" column="delFlag"/>
    </resultMap>
    <insert id="shortLinkStats" parameterType="org.yzh.project.dao.entity.LinkAccessStatsDO">
        INSERT INTO
        t_link_access_stats
        (full_short_url,gid,date,pv,uv,uip,hour,weekday,create_time,update_time,del_flag)
        VALUES(#{linkAccessStats.fullShortUrl},#{linkAccessStats.gid},#{linkAccessStats.date},#{linkAccessStats.pv},#{linkAccessStats.uv},#{linkAccessStats.uip},#{linkAccessStats.hour},#{linkAccessStats.weekday},NOW(),NOW(),0)
        ON DUPLICATE KEY
        UPDATE pv =pv+#{linkAccessStats.pv},uv=uv+#{linkAccessStats.uv},uip=uip+#{linkAccessStats.uip};
    </insert>

</mapper>